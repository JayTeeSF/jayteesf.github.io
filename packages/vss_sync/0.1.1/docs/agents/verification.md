# Verification

```sh
./bin/verify
```

Requires package manifest verification, executable Hey specs, help contracts,
and source-handoff integrity. Consumer integration remains a separate app/server gate.
