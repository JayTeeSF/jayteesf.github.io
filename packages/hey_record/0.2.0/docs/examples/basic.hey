# Import hey_record plus one adapter package in an application.
import '../main.hey'

program
  let dialect = HeyRecordDialect.sqlite3()
  let plan = HeyRecordSql.select_plan(dialect, 'cards', ['id', 'name'], {active: true}, '"name" ASC', 20, 0)
  says plan.sql
  says plan.parameters
end
