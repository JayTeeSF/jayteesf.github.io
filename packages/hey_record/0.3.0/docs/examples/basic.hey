import '../../main.hey'

program
  let dialect = HeyRecordDialect.sqlite3()
  let plan = HeyRecordSql.select_plan(dialect, 'cards', ['id', 'name'], {active: true}, '"name" ASC', 20, 0)
  says HeyRecordInfo.version()
  says plan.sql
end
