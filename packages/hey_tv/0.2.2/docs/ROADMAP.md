# Roadmap

1. Prove target-neutral public display and remote-action values.
2. Add stable accessibility and layout-hint values only when both Apple TV and
   Android/Google TV require the same application-neutral contract.
3. Keep QR bitmap rendering and native UI in platform packages.
