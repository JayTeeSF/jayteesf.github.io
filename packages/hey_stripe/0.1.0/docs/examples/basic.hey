# Pure documentation example (no network): inspect the package capabilities.
import '../../main.hey'

program
  says HeyStripeInfo.version()
  says HeyStripeInfo.capabilities().webhook_signature_verification
  says HeyStripeInfo.capabilities().checkout_sessions
end
