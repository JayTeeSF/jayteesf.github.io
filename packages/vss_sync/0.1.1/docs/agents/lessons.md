# Lessons

- Domain protocol code can be reusable without belonging in stdlib.
- A package should own pure semantics, not transport, persistence, platform capabilities, or product policy.
- Consumers must install immutable verified bytes; editable sibling imports are not release evidence.
