document.addEventListener('DOMContentLoaded',()=>{const search=document.querySelector('[data-search]');if(search){search.addEventListener('input',()=>{const q=search.value.trim().toLowerCase();document.querySelectorAll('[data-card]').forEach(card=>{card.hidden=q && !card.textContent.toLowerCase().includes(q)})})}const button=document.querySelector('[data-reveal]');if(button){button.addEventListener('click',async()=>{if(!window.confirm('Reveal the reference Hey solution? Try the challenge first; there are many valid approaches.'))return;button.disabled=true;button.textContent='Loading solution…';try{const response=await fetch(button.dataset.solution,{cache:'no-store'});if(!response.ok)throw new Error('HTTP '+response.status);const source=await response.text();const target=document.querySelector('[data-solution]');target.querySelector('code').textContent=source;target.classList.add('visible');button.textContent='Solution revealed'}catch(error){button.disabled=false;button.textContent='Reveal reference solution';alert('Unable to load the solution: '+error.message)}})}});