# Roadmap

1. v0.1.0: pure vss-sync-3 identity, routes, validation, and conflict selection.
2. Install the exact verified archive into app and server and remove duplicated pure logic.
3. Add cross-repository byte-equivalent fixtures and package lockfile gates.
4. Keep HTTP/auth/storage/native capabilities outside this package.

5. Add Android and browser clients as additional immutable consumers without adding platform code here.
