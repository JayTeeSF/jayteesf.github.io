# In progress

## v0.1.1 multi-client package handoff

The pure package remains protocol-specific and platform-neutral. Its next
executable milestone is immutable package consumption by both VSS app and
server, after which Android and browser clients can consume the same contract.
